{
  "name": "xit",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "surge ./dist --domain xit.surge.sh"
  },
  "devDependencies": {
    "vite": "^5.0.8"
  }
}
